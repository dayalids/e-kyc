import { usePermissions } from './usePermissions'
import ProtectedRoutes from './ProtectedRoutes'
import { ROLE, permissions, routes } from './config.Js'

export { usePermissions, ProtectedRoutes, ROLE, permissions, routes }
